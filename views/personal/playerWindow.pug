#playerWindow
    #playerLoader
        .videoWr
            .videoWrContent
                .videoWrContentLoader
                    .videoWrContentLoaderBox(style="width:30%")
                        include ../svg/loader.svg
    #playerBox(style="display:none")
        .playerPoster(v-if="!status.status")
            .videoWr
                .videoWrContent
                    img(:src="'/static/image/hi/'+status.poster_ru")
                //.playerPosterAlert
                    .playerPosterAlertLid {{status.alert_lid_!{lang}}}
                    .playerPosterAlertText {{status.alert_title_!{lang}}}
        div(v-else)
            .playerHead
                .playerBtnWr
                    .playerBtn
                        .playerBtnIco
                        .playerBtnText Пленарная Сессия
                .playerBtnWr
                    .playerBtn
                        .playerBtnIco
                        .playerBtnText зал 1
                .playerBtnWr
                    .playerBtn
                        .playerBtnIco
                        .playerBtnText зал 2
                .playerBtnWr
                    .playerBtn
                        .playerBtnIco
                        .playerBtnText зал 3
                .playerBtnWr
                    .playerBtn
                        .playerBtnIco
                        .playerBtnText зал 4

            .playerBody !{JSON.stringify(test)}


