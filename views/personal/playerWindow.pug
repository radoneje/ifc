#playerWindow
    #playerLoader
        .videoWr
            .videoWrContent
                .videoWrContentLoader
                    .videoWrContentLoaderBox(style="width:30%")
                        include ../svg/loader.svg
    #playerBox(style="display:none")
        .playerPoster(v-if="!status.status")
            .videoWr
                .videoWrContent {{status.poster_ru}}
                    img(:src="'/static/image/middle/'+status.poster_ru")

        .playerHead
            .playerBtnWr
                .playerBtn
                    .playerBtnIco
                    .playerBtnText Пленарная Сессия
            .playerBtnWr
                .playerBtn
                    .playerBtnIco
                    .playerBtnText зал 1
            .playerBtnWr
                .playerBtn
                    .playerBtnIco
                    .playerBtnText зал 2
            .playerBtnWr
                .playerBtn
                    .playerBtnIco
                    .playerBtnText зал 3
            .playerBtnWr
                .playerBtn
                    .playerBtnIco
                    .playerBtnText зал 4

        .playerBody !{JSON.stringify(test)}


