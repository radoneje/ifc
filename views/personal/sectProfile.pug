.participaintRow
    .menuItem.flex.center.left
        .menuItemBox
        .menuItemText Профиль
.regRow
    .reqPhoto2.reg287(v-on:click="uploadPhoto()")
        .reqPhotoBox2
            .reqPhotoBox2Photo
                img(v-if="user.photoid" :src="'/static/image/middle/'+user.photoid")
    .reg287.mt2.pBtnChange(v-on:click="uploadPhoto()") Изменить фото
.regRow()
    .regRowGroup.readonly.flex.left
        .recLabel.reg287 Фамилия
        input.reqInput(readonly v-model="user.f" must name="f")
.regRow()
    .regRowGroup.readonly.flex.left
        .recLabel.reg287 Имя
        input.reqInput(readonly v-model="user.i" must name="i")
.regRow()
    .regRowGroup.readonly.flex.left
        .recLabel.reg287 Отчество
        input.reqInput(readonly v-model="user.o" must name="o")
.regRow()
    .regRowGroup.readonly.flex.left
        .recLabel.reg287 паспорт серия номер
        input.reqInput(readonly v-model="user.passportNumber" must name="passportNumber" placeholder="ФОРМАТ: С ПРОБЕЛОМ, КАК В ДОКУМЕНТЕ")
.regRow()
    .regRowGroup.readonly.flex.left
        .recLabel.reg287 дата выдачи
        input.reqInput(readonly v-model="user.passportDate" must name="passportDate" placeholder="ФОРМАТ: КАК В ДОКУМЕНТЕ")
.regRow()
    .regRowGroup.flex.left
        .recLabel.readonly.reg287 код подразделения
        input.reqInput(readonly v-model="user.passportCode" must name="passportCode" placeholder="ФОРМАТ: КАК В ДОКУМЕНТЕ")
.regRow()
    .regRowGroup.flex.left
        .recLabel.readonly.reg287 адрес регистрации
        input.reqInput(readonly v-model="user.address" must name="address" placeholder="ФОРМАТ: КАК В ДОКУМЕНТЕ")
.regRow()
    .regRowGroup.flex.left
        .recLabel.reg287 email
        input.reqInput(v-model="user.email" must name="email" email )
.regRow()
    .regRowGroup.flex.left
        .recLabel.reg287 телефон
        input.reqInput(v-model="user.phone" must name="phone" placeholder="ФОРМАТ: +7 000 000 0000")

